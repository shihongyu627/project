// initial state
const baseName = '/api/user'

const userInfo = baseName + '/info'
const userProjectlist = baseName + '/projectlist'
const userTeamlist = baseName + '/teamlist'
const userLogout = baseName + '/logout'
const delsign = baseName + '/delsign' //删除签名
const signlist = baseName + '/signlist' //签名列表

export {
  userInfo,
  userProjectlist,
  userTeamlist,
  userLogout,
  delsign,
  signlist
}

