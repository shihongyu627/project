// initial state
const baseName = '/api/message'

const messageBadge = baseName + '/badge'
const messageLists = baseName + '/lists'
const messageRead = baseName + '/read'

export {
  messageBadge,
  messageLists,
  messageRead,
}

