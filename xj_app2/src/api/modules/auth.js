// initial state
const baseName = '/api/auth'
const authLogin = baseName + '/login'
const authCheck = baseName + '/check'
const authchangePassword = baseName + '/changepassword'
const sendsms = baseName + '/sendsms'

export {
  authLogin,
  authCheck,
  authchangePassword,
  sendsms
}

