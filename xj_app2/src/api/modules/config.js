// initial state
const baseName = '/api/config'

const configAppWelcome = baseName + '/getAppWelcomePageConfig'
const configWebsite = baseName + '/webSite'
const checkUpdate = '/api/app' + '/check'

export {
  configAppWelcome,
  configWebsite,
  checkUpdate
}

