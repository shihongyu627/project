// initial state
const baseName = '/api/project'

const projectLists = baseName + '/lists'
const projectDetail = baseName + '/detail'
const projectDroplist = baseName + '/droplist'
const contactlists = baseName + '/contactlists'

export {
  projectLists,
  projectDetail,
  projectDroplist,
  contactlists
}

