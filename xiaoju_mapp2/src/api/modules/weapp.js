// initial state
const baseName = '/api/wechat'

const alipayName = '/api/alipay'



const weappCheck = baseName + '/check'
const weappLogin = baseName + '/login'
const weappDecrypt = baseName + '/decrypt'

const alipayCheck = alipayName + '/check'
const alipayLogin = alipayName + '/login'


export {
  weappCheck,
  weappLogin,
  weappDecrypt,
  alipayCheck,
  alipayLogin
}
